<div id="legendary-counts">
    <h3>Legendaries in set - {{ stats.legendaryCounts.total }}</h3>

    {# Table breaking down legendary counts by types and rarity #}

    {% for type, amount in stats.legendaryCounts.counts %}
        {{ amount }}x {{ type }}
    {% endfor %}
</div>