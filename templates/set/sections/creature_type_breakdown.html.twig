<div id="creature-types-breakdown">
    <h3>Creature types breakdown</h3>
    <div class="top3">
        <div class="top3-block">
            {% for type, amount in stats.creatureTypesStats.stats|slice(0,3) %}
                <div class="top3-item">
                    <span class="amount">{{ amount }}x</span>
                    <br>
                    <span class="type">{{ type }}</span>
                </div>
            {% endfor %}
        </div>
    </div>

    <div class="other-types">
        {% for type, amount in stats.creatureTypesStats.stats|filter(v => v > 1) %}
            <div>
                <div class="label">{{ type }}</div>
                <div class="div-hx div-h-{{ amount }}"></div>
                <div class="amount">{{ amount }}</div>
            </div>
        {% endfor %}
    </div>

    <div class="one-ofs">
        <h4>One-offs</h4>
        {% for type in stats.creatureTypesStats.oneOffs %}
            {{ type }}{% if not loop.last %}, {% endif %}
        {% endfor %}
    </div>

    <div class="info">Different types - {{ stats.creatureTypesStats|length }}</div>
</div>